import clsx from "clsx";
import {
  ReactNode,
} from "react";

import styles from "./{{properCase name}}.module.scss";

export const {{properCase name}} = (): ReactNode => {

  return (
    <div className={clsx(styles.{{properCase name}})}
    >
      no impl.
    </div>
  );
};
