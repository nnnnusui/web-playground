import type { Meta, StoryObj } from "@storybook/react";

import { ScrollBarTo } from ".";

const meta: Meta<typeof ScrollBarTo> = {
  component: ScrollBarTo,
  args: {
    
  },
};

export default meta;
type Story = StoryObj<typeof ScrollBarTo>;

export const Default: Story = {
};
